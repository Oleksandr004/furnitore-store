import styles from '@/components/product-page/AboutProduct/Reviews/Reviews.module.scss'

const Reviews = () => {
	return (
		<section className={`${styles.reviews}`}>
			<div className={`${styles.container}`}>No Info</div>
		</section>
	)
}

export default Reviews
